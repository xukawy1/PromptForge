# Phase 12.1 状态说明（v0.6.0 四轮用户反馈）

1. 采集中心：
   - 本地文件采集新增"识别与归纳"（同网页：正文 + 图片 OCR → 普通格式提示词，可编辑、可重新识别）；
   - 归纳结果框加大（最小高度 300，随窗体自动缩放铺满）；
   - 手工文本/本地文件/网页三类保存到知识库全部改为弹窗选择分类与名称（必选校验，未选分类不能保存），
     保存成功后状态区显示"已保存到知识库「分类」下的「名称」"；
   - "本次采集结果"移入独立窗体：主页点"查看采集结果"打开结果详情窗体，内含 AI 规整/关键词规整
     （先预览可编辑）+ 重新规整 + 保存到知识库（选分类），主页面保留自动规整开关；
2. Skill 库扩充到 21 个：新增 8 个视频/广告类官方 skill（3D 动画短片、品牌宣传片、双人游戏开场、
   手绘实拍、极简产品广告、音乐视频字幕、纸艺拼贴、定格纸艺）+ awesome-chatgpt-prompts CSV 版；
   skill 源目录随启动自动安装（data/skills）；
3. 翻译空显示修复：思考型模型输出整体落在 <think> 内导致清洗后为空——统一 clean_llm_text
   （优先取 </think> 之后内容），生成/翻译/规整/OCR/视觉反推全部换用；
4. Prompt 生成：系统提示词强化"选择模板时严格按模板结构与变量语义详细扩写"；新增"保存到知识库…"
   （必选分类与名称，未选不能保存，保存成功显示分类与名称）；
5. 模型提示统一：所有调用大模型处未配置默认模型时，统一提示"请打开「模型中心」测试连接并设置默认模型"；
   默认模型本就持久化于 config（重启保持上次选择），换模型需到模型中心手动切换；
6. 历史记录页重排：标题 + "任务状态筛选"分组（状态下拉/刷新/重试）在上，历史表格与任务详情
   上下分区在下，表格斑马纹、表头自适应；
7. 背景应用修复与重启机制：点击"保存并应用背景（软件将自动重启刷新）"时，若有任务进行会先阻止
   并提示等待；确认后保存配置并自动重启一次软件加载整页背景（透明浮层模式）。

测试：60 个测试全绿，12 个页面冒烟通过。
