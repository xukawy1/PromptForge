# Phase 15.5 状态说明（v0.8.4）

1. 采集结果窗体左栏拆分：上方「① 原文预览」（可编辑，分析内容从这里提取，不再被替换），
   下方「② 综合提示词」（综合提示词规整/扩写的结果，独立展示与保存）；
2. 提示词拆分修复与增强：
   - 多策略 JSON 解析（标准 → 去代码块/修尾逗号 → 逐对象正则兜底，截断输出也能恢复条目）；
   - 长文自动分段分析（按行切分 ≤3200 字符/段、最多 6 段，逐段调用模型后合并去重，状态栏显示分段数）；
   - 图片内容参与拆分：拆分会自动对来源图片做 OCR（最多 3 张）并入分析文本，每张图片内容单独成条并分类；
3. 图片缩略图：左栏原文下方展示来源图片小缩略图，点击弹出可缩放的放大预览窗；
4. 速度优化：打开结果窗体时后台静默预热模型（num_predict=1 预热请求），配合 keep_alive 与
   分段小请求（更快的 prefill），首次点击「获取提示词」等待明显减少；
5. 其余功能零改动；75 个测试全绿（新增解析容错、分段切分、分段合并去重 3 组用例），
   带图窗体冒烟（双框独立/缩略图/放大预览）通过。
