# Phase 15.1 状态说明（v0.8.0）

采集中心网页提示词整理重构：
1. 自动提示词拆分：新增 KeywordOrganizeService.extract_prompts——打开「采集结果详情」即自动把内容拆分为
   「1 条综合总结提示词 + 多个分散提示词」（LLM 结构化输出，含中文短名、英文正文与所属类别；
   类别限定为 人物/场景/摄影/镜头语言/光线/构图/色彩/风格/视频/声音/质量/其他，非法值归为「其他」；
   非 JSON 输出时有兜底综合提示）；
2. 新窗体「提示词清单（自动）」页：综合总结卡片（高亮）+ 每条提示词卡片（名称可改、类别可选、正文可编辑、
   可勾选）；按钮：全选/全不选、大模型扩写选中项（最多 8 条）、保存选中到知识库、一键保存全部；
3. 按类别自动归档：保存时按卡片类别写入对应知识分类（如「人物」条目全部进「人物」分类），
   名称用卡片标题（分散提示词以关键字命名）；
4. 重复导入覆盖：保存前自动清理同一来源/同一 URL 的旧抽取记录（source_type 为
   extracted_prompt/multi_style），并回报「已覆盖 N 条旧记录」；
5. 采集中心排版优化：顶部操作行（自动规整开关 + 查看采集结果）+ 一行式说明；左栏采集方式/历史
   （比例 280/720）；修复历史条目点击因引用已删控件导致的崩溃（现在显示摘要并提示查看结果入口）；
6. 旧「多风格识别」页签由「提示词清单（自动）」统一承担（类别维度覆盖风格维度）；
7. 其余功能零改动；新增 Phase 15 专项测试（拆分解析与兜底、分类归一、按类别保存、覆盖去重、
   同 URL 覆盖、扩写），70 个测试全绿。
