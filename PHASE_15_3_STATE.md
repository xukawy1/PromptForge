# Phase 15.3 状态说明（v0.8.2）

1. 大模型调用加速：
   - Ollama 请求携带 keep_alive=30m，模型常驻显存，后续调用无需重新加载（大模型二次调用提速最明显）；
   - Ollama/OpenAI兼容 Provider 改为共享 HTTP 连接池（避免每次调用重新握手）；
   - 拆分/扩写/综合规整改为流式生成，边生成边推进进度；
2. 任务进度可视化：采集结果窗体的拆分/扩写/综合规整任务显示进度条 + 「已用时 Ns」实时计时；
3. 模型缺失引导：凡调用大模型处未配置默认模型时，弹出「去模型中心设置」按钮，一键跳转到模型中心
   （覆盖采集结果窗体、Prompt 生成、Skill 工坊、图片反推、采集中心）；
4. 工作台新增「一键释放内存」：清理 Python 缓存、界面图片缓存与空闲网络连接，弹窗显示
   释放前/后占用与释放量（Windows 进程工作集统计）；
5. 其余功能零改动；72 个测试全绿。
