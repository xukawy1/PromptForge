# Phase 9.1 状态说明（v0.4.0 二轮用户反馈）

1. 采集中心重构：左侧"采集方式"分类导航（文本/文件/网页，点击切换右侧对应表单，不再拥挤）
   + 左下方"采集历史"列表；底部保留结果、内容预览与任务进度；
2. 采集内容智能规整同步知识库：KeywordOrganizeService 对采集正文做关键词提取（英文整词 + 中文 2-gram
   词频，内置停用词，无需外部分词依赖），按中英关键词映射自动匹配知识分类（人物/光线/构图/色彩/质量等），
   生成含"规整摘要/关键词索引/自动分类/提示词要素/原始正文"的知识卡片写入 knowledge_items
   （source_type=collector_source 保留来源追溯，按来源去重）；可选"AI 规整"调用默认大模型生成总结；
   支持采集完成后自动规整（设置项 auto_organize，结果区可勾选）；
3. Prompt 库 ↔ 知识库同步：新增/编辑 Prompt 时可勾选"同时保存为知识条目"并选择知识分类、知识名称；
   详情区新增"同步到知识库…"按钮（同样可选分类）；页面新增刷新按钮并支持切换页面自动刷新；
   知识库新增分类后回到 Prompt 库即可在分类下拉中选择；
4. Skill 工坊（新页面，Migration 2 新增 skills 表）：
   - 一键安装：支持 .md/.markdown/.txt 文件或整个文件夹（自动合并其中 skill 文件，上限 20 文件/200KB），
     关键字取文件/文件夹名，重复安装自动更新；
   - 点击左侧关键字目录查看 skill 书写格式全文；
   - 选择知识库提示词素材 → "按 Skill 格式生成"：有默认 LLM 时以 skill 内容为 system 让模型改写为该格式成品，
     无模型时规则拼接（明确标注未调用模型）；
   - 结果可"保存为 Prompt"（哈希去重）或"保存到知识库…"（可选分类，source_type=skill_result）；
   - 素材选择随页面切换自动刷新；
5. 背景图片升级：按当前窗口尺寸 cover 裁剪铺满整个页面背景，遮罩浓度滑杆（0-90%）混合窗口底色保证可读性，
   窗口缩放防抖后自动重生成适配图（缓存于 data/cache）；设置页显示建议尺寸说明。

测试：新增 Phase 9 专项测试（skills 表迁移、skill 安装/更新/规则模式/LLM 模式/双路保存、
关键词提取、分类匹配、规整去重、AI 增强规整），56 个测试全绿，12 个页面冒烟通过。
