# Phase 3.18 状态说明

本阶段完成启动恢复策略与用户确认基础：
- 历史记录页的“重试选中任务”支持跨重启恢复采集任务；
- 优先使用当前进程执行器，执行器失效时读取持久化 execution_plan；
- 仅允许文本、文件、网页采集三类固定操作；
- 严格校验参数类型、参数数量上限、任务类型和计划版本；
- 修复 allow_existing 重试时重复创建任务记录的问题；
- 主窗口向历史记录页注入 CollectorService；
- 未自动启动历史任务，仍由用户在历史记录页确认重试，避免启动时意外执行。
