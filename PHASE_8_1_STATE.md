# Phase 8.1 状态说明（v0.3.0 用户反馈优化）

本阶段根据用户 9 点反馈完成界面与功能升级：
1. 应用图标：程序化绘制靛紫渐变 + P 字 + 星光图标，生成多尺寸 ico，exe 与窗口标题栏均使用；
2. 主题系统重写为完整 QSS：亮/暗两套基础色 + 12 种强调色预设 + 自定义色，卡片圆角、按钮悬停、
   导航高亮、表格表头、滚动条等全套控件样式；
3. 设置页新增背景图片：可选本地图片作背景，支持"加深遮罩"提升可读性（自动生成加暗缓存），
   背景应用在页面留白处，内容卡片保持底色；关于区显示版本与数据目录；
4. 工作台：新增"刷新统计"按钮，并支持切换到工作台时自动刷新；统计卡片使用统一卡片样式；
5. 采集中心：新增"内容预览与采集历史"面板——采集完成后立即显示正文预览；历史列表列出最近 50 条来源，
   点击任意条目可回看该次采集的完整内容（文档正文或图片信息）；页面切换时自动加载历史；
   CollectorService 新增 recent_sources / load_source_content；
6. Prompt组件 / Prompt模板页：顶部新增页面用途与用法说明；
7. 新增"Prompt 库"独立页面（主导航可直接进入）：分页搜索浏览全部已保存 Prompt、
   详情展示（正向/负向/中文翻译/参数/元信息/来源追溯）、新增/编辑/删除；
8. Prompt生成：流式输出实时进度（进度条 + 已生成字符数 + 实时显示生成内容）；
   生成结果分 English / 中文翻译 双标签；系统提示词要求模型双语输出；
   保存到 Prompt 库时中文翻译一并写入 analysis_result；
   GenerationService/OllamaProvider 新增 generate_stream 流式接口与双语 parse_result；
9. 图片反推：支持把图片拖入页面导入；新增图片预览；新增"AI 视觉反推"——
   参照 ComfyUI-llama-TE 工作流（图片→本地多模态模型→提示词）原理，通过 Ollama 视觉模型看图反推，
   优先使用默认 Vision 模型、回退默认 LLM；反推指令可自定义（如面向视频提示词的反推）；
   AI 结果与 Metadata 解析结果均可保存入库并保留图片溯源；
   OllamaProvider 新增 vision 接口（图片 base64 上传）；
10. 其余优化：页面标题统一为 pageTitle/pageSubtitle 样式；providers 包导入修复等。

测试：新增 Phase 8 专项测试（视觉请求负载、视觉反推链路、采集历史预览、流式生成、双语解析与入库）。
