# Phase 16.4 状态说明（v0.8.11）

Skill 工坊素材区新增两个「一键清空」按钮：
1. 提示词素材（列表选择）标题行：一键清空 → 取消当前选中的素材并清空预览，避免与手动输入内容混淆；
2. 手动输入素材标题行：一键清空 → 清空手动输入框；
3. 清空后状态栏分别提示（如"已清空素材选择。（手动输入内容仍将优先生效）"），便于用户明确当前生效来源；
4. 其余功能零改动；83 个测试全绿，清空交互冒烟（清空前手动优先/预览非空 → 清空选择后预览空 → 清空输入后为空 → 两者清空后素材为空）通过。
