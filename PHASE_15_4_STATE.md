# Phase 15.4 状态说明（v0.8.3）

1. 右键菜单修复：全局样式表缺失 QMenu/QMenuBar 样式导致右键复制粘贴菜单黑底黑字不可用；
   新增 QMenu/QMenuBar 完整配色（surface 底、text 字、accent 选中、分隔线），亮/暗与背景图模式均覆盖，
   离屏采样验证：菜单白底 + 深色文字像素，可读可点；
2. 采集结果详情窗体改为左右布局（取消页签）：
   - 左：原文 / 规整预览（可编辑）——「综合提示词规整」「扩写综合提示词」「保存到知识库…」；
     综合提示词规整改为读取左侧框当前文字（可用 expand 指令汇总），不再依赖固定来源；
   - 右：提示词清单——取消自动提取，用户手动点「获取提示词（分析左侧文字）」后模型才开始分析；
     每次获取先清空旧卡片再重建；
3. 拆分识别规则强化（extract_prompts_from_text 新增）：
   - 按序号（1. / 2. / ①② / 一、二、）或关键字标题逐条拆分，序号/标题直接用于命名；
   - 含图片识别文字（[图片·文件名]）时，按每张图片内容分别提取并判断归类，不同图片不得合并；
4. 其余功能零改动；72 个测试全绿，窗体冒烟（左原文/右未自动/手动按钮）通过。
