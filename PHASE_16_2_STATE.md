# Phase 16.2 状态说明（v0.8.9）

修复 Skill 工坊两个问题：
1. 进入页面卡顿：原因是每次进入都全量读取素材（含正文）并逐条查询分类名（数百次数据库查询）。
   修复：新增 SkillService.list_material_index（仅取 id/标题/分类/时间，正文按需再取）与
   category_name_map（一次查询分类名映射）；页面改为懒加载（首次进入才加载一次），
   预览与生成时按需读取正文并加「刷新素材」按钮。实测 D 盘真实库（400 条素材）索引加载 3.6ms；
2. 生成结果为空：原因是部分 skill 文档较长（可达数万字），整段作为系统提示词超出模型上下文，
   模型返回空内容而界面仍显示完成。修复：apply_skill 三重防护——
   ① 系统提示词按核心规范节选（优先 SKILL.md，限 6000 字，兜底说明附后）；
   ② 模型返回为空自动用更短提示词（2500 字规范）重试一次；
   ③ 仍为空则规则拼接兜底（mode=rule_fallback），结果永不空白并在状态栏提示原因与建议；
   页面增加空结果保护提示；
3. 其余功能零改动；82 个测试全绿（新增规范节选优先级、空结果兜底、素材索引/按需正文/分类映射用例）。
