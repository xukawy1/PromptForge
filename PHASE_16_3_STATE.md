# Phase 16.3 状态说明（v0.8.10）

Skill 工坊两项优化：
1. 已安装 Skill 支持重命名：新增「重命名」按钮（下拉列表名称可直接修改）；
   SkillService.rename_skill 校验非空、长度（≤60 字）与重名冲突后更新 keyword/name；
   同时修正启动自动安装逻辑——按来源路径跳过已安装项，用户重命名后重启软件不会被重复装回旧名副本；
2. 提示词素材支持手动输入：素材区新增「手动输入素材」框，填写后优先生效（未填写时回退到列表中选择的知识条目），
   点击「按 Skill 规范生成」即结合所选 Skill 格式对输入内容进行详细扩写；提示文案同步更新；
3. 其余功能零改动；83 个测试全绿（新增重命名成功/持久化/重名拒绝/空名校验/重命名后启动不重复安装用例）。
