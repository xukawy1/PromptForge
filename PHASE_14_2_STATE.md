# Phase 14.2 状态说明（v0.7.1）

1. 模型中心修复与加固：
   - 修复 list_models 参数误用导致「本地记录回退」读不到模型的问题；
   - 页面打开即从本地模型表加载此前发现过的模型（Ollama 未启动也能看到并选择）；
   - Ollama 连接失败时弹窗提示（如何启动 Ollama / 可改用 API 接入），失败后表格回退展示本地记录；
   - 点击模型行自动记住为默认 LLM（v0.7.0 引入）保持不变。
2. 多风格识别入口调整：从采集页按钮移入「查看采集结果」详情窗体——
   窗体改为双页签（规整预览 / 多风格识别（自动）），打开即自动分析来源内容是否含多种提示词风格，
   多种则分类为卡片（可编辑、可勾选）供选择保存；单一种类会明确提示"未发现多种风格"；
   保存仍走必选分类弹窗；移除采集页上的独立按钮，避免入口重复。
3. 其余功能零改动；65 个测试全绿，12 页冒烟通过。
