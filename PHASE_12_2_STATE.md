# Phase 12.2 状态说明（v0.6.1 五轮用户反馈修复）

1. 背景保存崩溃修复：main_window 顶部补 Path 导入（NameError 根因），_prepare_background
   全程异常保护（图片处理失败不再阻断启动）；
2. 采集中心：
   - 取消底部"内容预览"框，归纳结果由网页/文件页各自的大文本框承担；
   - "采集完成后自动规整到知识库"与"查看采集结果"上移到标题下方操作行；
   - 结果窗体规整逻辑重写：新增 KeywordOrganizeService.build_prompt_card——
     把采集内容（正文+图片 OCR）真正规整为"可直接用于 AI 图片/视频生成的提示词卡"
     （【English】/【中文】/Negative prompt/建议参数，剔除广告导航等无关内容），
     LLM 模式直接输出成品，无模型时输出基础骨架并提示；
3. Skill 工坊：下拉框下方新增"已安装 N 个，关键字：…"显示区；修复打包脚本
   排除了 data/skills 导致用户侧安装不到 skill 的根因（现在 exe 包内含全部 skill 源）；
4. Prompt 生成：结果区简化为单个"扩写提示词"框（移除双语标签页），模板严格扩写指令保持。
