# Phase 15.8 状态说明（v0.8.7）

图片反推模块新增删除功能：
1. 服务层 ImageAnalysisService.delete_image：删除图片记录；若图片文件位于软件数据目录内
   （data/knowledge/images）则连同本地副本一并删除，用户原始文件不受影响；返回 file_removed 标记；
2. 图片反推页新增「删除选中图片」按钮：选中待解析列表中的图片 → 确认弹窗
   （说明将移除列表项、删除软件内副本、不影响原始文件、已保存的 Prompt 仅解除图片关联）→
   删除后自动清理当前预览与详情并刷新列表，状态栏提示是否含本地副本清理；
3. 其余功能零改动；78 个测试全绿（新增数据目录内文件随删 / 目录外文件保留两种路径用例）。
