# Phase 3.10 状态

本阶段完成 Phase 3 采集中心整体验收准备：

- 统一任务查询接口：`get_task`、`list_tasks`
- 增加任务重试资格检查：`can_retry`
- 增加明确的 `retry_task` 接口，保留兼容旧接口 `retry`
- 历史页面调用统一重试接口
- 增加任务列表、重试次数、重试上限专项测试
- 完成代码编译检查

说明：当前重试会安全地将失败/取消任务恢复为 pending，并递增 retry_count；真正重新执行由后续统一任务执行器负责，避免根据字符串任务类型误调用业务函数。
