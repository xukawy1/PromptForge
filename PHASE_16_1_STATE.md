# Phase 16.1 状态说明（v0.8.8）

历史记录新增删除功能：
1. TaskService 新增 delete_task（单条删除，进行中/排队中的任务拒绝删除并提示）与 clear_history
   （清空全部已结束记录，进行中/排队中自动保留并回报数量）；
2. 历史记录页新增「删除选中记录」与「清空全部历史」按钮：
   - 单条删除：确认弹窗展示任务ID/类型/状态，并说明范围（不影响已采集数据与知识库）；
   - 清空全部：二次确认弹窗列明条数与影响（保留执行中任务、不影响知识库/Prompt库/Skill、不可恢复）；
   - 删除后自动刷新列表与详情区；
3. 其余功能零改动；79 个测试全绿（新增单条删除/进行中保护/清空保留执行中用例）。
