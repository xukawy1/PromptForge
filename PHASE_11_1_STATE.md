# Phase 11.1 状态说明（v0.5.0 三轮用户反馈）

1. 网页采集识别归纳闭环：新增 summarize_web_source——正文 + 图片文字（Vision OCR，优先默认 Vision
   模型、回退默认 LLM，最多识别 3 张图）→ 归纳为普通格式提示词描述（默认 LLM 润色，无模型降级关键词规整）；
   采集中心网页页新增"识别图片文字并归纳 / 重新识别归纳 / 保存到知识库（选分类）"，归纳结果可直接编辑；
2. 内置内容自动就位：程序启动即静默导入内置提示词包（15 知识卡 + 12 成品 Prompt，按标题/哈希幂等），
   知识库各分类目录下直接可见，无需手动点击导入（按钮保留）；
3. 知识库划词翻译：正文中选中文字 → 下方显示选中预览 → 选择目标语言（18 种）→ 一键翻译
   （GenerationService.translate，TaskManager 异步，think 块剥离）；
4. Skill 工坊：已安装列表改下拉菜单节约空间；素材选择改"分类 → 条目"两级（与知识库一致）；
   修复生成截断问题（Ollama 默认 num_predict=128 → 全局默认 2048，skill 扩充 3072）；
   生成指令升级为"识别 skill 规范 + 详细扩充素材"；结果区改"生成结果 / 翻译译文"双标签 + 一键翻译（18 语言）；
5. 背景图真正整页生效：新增背景模式覆盖样式（pageHost 下所有控件透明、内容控件半透明浮层、
   按钮半透明），像桌面端应用一样整页背景图 + 半透明浮层保证可读；设置页新增"保存并应用背景"按钮；
6. 预置组件与模板：内置 8 个组件（电影感光线/浅景深/赛博朋克/黄金时刻/超精细/低角度/青橙调色/雨夜，
   各含 3-4 条写法变体）+ 5 个模板（人像摄影/风景大片/产品商业图/H3 视频分镜/扁平 LOGO，含变量与默认值），
   启动自动导入（按 canonical_name/name 幂等）。

测试：新增 Phase 11 专项测试（网页 OCR 归纳与入库、翻译服务、组件/模板种子幂等），
60 个测试全绿，12 个页面冒烟通过。
