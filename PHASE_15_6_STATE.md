# Phase 15.6 状态说明（v0.8.5）

1. 提示词拆分引擎强化（确保必出清单）：
   - EXTRACT/CHUNK 提示词升级为八仙级 few-shot（"资料含八仙就要输出 8 条 items，title 为八位角色名"）；
   - 长文多线程分段：每段 ≤3200 字、最多 8 段，API 模型 3 线程 / 本地模型 2 线程并发分析后合并去重；
   - 每段失败自动重试（换成"每行一个 JSON"的极简提取提示词）；仍失败则整段兜底成一条提示词，绝不丢内容；
   - 图片逐张成条：每张图片 OCR 后按内容生成条目（title 用图片内容关键词、类别按内容判断），
     若模型未覆盖该图片则自动补条，几张图就至少几条提示词；
   - 新增独立综合总结生成（512 token 短请求）；
2. 模型中心：
   - API 配置多套保存：可保存多套（名称 + 厂商 + 地址 + 密钥），下拉切换「应用选中配置」；
   - **删除功能**：删除即从本机移除该配置与密钥；若删除的是当前启用配置，同时清空当前 API 凭据（防他人套用）；
   - 打开页面自动加载本地记录的模型清单并高亮上次的默认模型；后台静默自动同步一次（失败不弹窗）；
3. 图片反推页：待解析图片列表改为**缩略图列表**（96px 缩略图 + 文件名/状态），点击缩略图在下方大图框显示该图片，即可直接进行反推；
4. 其余功能零改动；76 个测试全绿（新增 API 配置保存/应用/删除用例）。
