# Phase 15.7 状态说明（v0.8.6）

模型中心 API 区重构为「供应商管理模式」：
1. 供应商列表：表格展示每个供应商的状态（★当前）、名称、厂商、Base URL、密钥（掩码）；
2. 添加供应商：点「添加供应商…」弹出录入窗（名称/厂商/Base URL/API Key），保存即新增一条记录并设为当前，
   自动刷新其模型清单；
3. 任意切换：选中记录点「设为当前供应商」（或「测试所选并刷新模型」）即切换启用；
4. 删除供应商（含后台记录）：确认后一次性完成——移除本机 Base URL 与密钥、**清除该供应商同步的全部
   模型记录（按 endpoint 匹配的 api:* 行）**；若为当前启用供应商则同时清空当前 API 凭据；
   若被清除的模型正是默认模型，同步重置默认设置（避免残留失效模型调用）；
5. delete_api_profile 返回值扩展：purged_models / cleared_defaults，UI 状态栏展示清理明细；
6. 其余功能零改动；77 个测试全绿（新增"删除供应商连带清除后台记录与重置失效默认"用例）。
