# Phase 3.13 状态说明

完成统一任务执行器的安全基础：
- 新增 TaskExecutionRegistry；
- TaskManager 在提交任务时登记任务类型、函数和参数；
- 新增 retry_registered()，仅允许重试当前进程内仍存在的已登记执行器；
- 执行器不存在时拒绝重试，避免从数据库字符串动态调用任意函数；
- 保持历史任务状态重置接口兼容。

限制：程序重启后，内存执行器会失效，历史页面会提示从采集中心重新提交。跨进程持久化执行器将在后续版本实现。
